<div class="wrapper">
  <%= render "#{@contribution.category}_prompt" %>
	<%= render "#{@contribution.category}_form" %>
	<%= render "s3_form", locals: { contribution: @contribution, policy_document: @policy_document } %>
	<%= render "server_form" %>

	<button id="submit">Submit</button>
</div>

<script id="prompt-url" type="text/plain">
  <%= s3_prefix + @contribution.parent.s3_id %>
</script>